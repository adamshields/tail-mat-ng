import { DisplayOnlyDirective } from './display-only.directive';

describe('DisplayOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
