import { BreakerColumnDirective } from './breaker-column.directive';

describe('BreakerColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new BreakerColumnDirective();
    expect(directive).toBeTruthy();
  });
});
